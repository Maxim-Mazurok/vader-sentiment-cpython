{
  "name": "vader-sentiment-cpython",
  "version": "0.0.1",
  "scripts": {
    "install": "node-gyp-build",
    "start": "bash ./start.sh",
    "prebuild": "prebuildify --napi --strip"
  },
  "dependencies": {
    "node-addon-api": "^3.1.0",
    "node-gyp-build": "^4.2.3"
  },
  "devDependencies": {
    "node-gyp": "^8.0.0",
    "prebuildify": "^4.1.2"
  }
}
