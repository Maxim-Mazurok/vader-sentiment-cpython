{
  "name": "vader-sentiment-cpython",
  "version": "0.0.1",
  "scripts": {
    "install": "bash ./build_boost.sh && bash ./build_python.sh && bash ./node-gyp-build.sh",
    "start": "bash ./install_vader.sh && bash ./start.sh",
    "test": "npm run prebuild && node test.js",
    "prebuild": "prebuildify --napi --strip",
    "node-gyp-build": "node-gyp-build"
  },
  "dependencies": {
    "node-addon-api": "^3.1.0",
    "node-gyp-build": "^4.2.3"
  },
  "devDependencies": {
    "node-gyp": "^8.0.0",
    "prebuildify": "^4.1.2"
  }
}
