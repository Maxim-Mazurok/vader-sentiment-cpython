{
  "name": "vader-sentiment-cpython",
  "version": "0.0.1",
  "scripts": {
    "install": "bash ./node-gyp-build.sh",
    "start": "bash ./start.sh",
    "test": "npm run prebuild && node test.js",
    "prebuild": "prebuildify --napi --strip",
    "node-gyp-build": "node-gyp-build",
    "node-gyp-build-test": "node-gyp-build-test"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0",
    "node-gyp-build": "^4.8.1"
  },
  "devDependencies": {
    "node-gyp": "^10.1.0",
    "prebuildify": "^6.0.1"
  }
}
